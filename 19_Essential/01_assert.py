# assert: Test if a condition returns True:
# if condition returns true Nothing happen, if false then exception raised

x = 'hello'
assert x == "hello"

# assert x == 'good bye' #AssertionError
# assert x != "hello", "x should be hello"

# The assert keyword is used when debugging code.